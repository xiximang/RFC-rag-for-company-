fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

celery>=5.3.0
redis>=5.0.0
aio-pika>=9.4.0

pymilvus>=2.3.0
pgvector>=0.2.5
psycopg2-binary>=2.9.9
boto3>=1.34.0
aiofiles>=23.0.0
python-multipart>=0.0.9

unstructured>=0.12.0
pdfplumber>=0.10.0
python-docx>=1.1.0
openpyxl>=3.1.0
pandas>=2.2.0

pillow>=10.2.0
requests>=2.31.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyahocorasick>=0.9.0
python-pptx>=0.6.23        # 2026-07-03: 启用 PowerPoint 解析
prometheus-client>=0.20.0

meilisearch>=0.31.0

httpx>=0.27.0
email-validator>=2.0.0
sse-starlette>=2.0.0

# ---------------------------------------------------------------------------
# Optional dependencies for enhanced parsing (commented out by default).
# Install them based on deployment needs; pipelines gracefully degrade if absent.
# ---------------------------------------------------------------------------

# OCR and scanned-PDF processing
# Requires system packages: tesseract-ocr (with chi_sim/eng language data) and poppler-utils
pytesseract>=0.3.10
pdf2image>=1.17.0

# Additional image format support
# pillow-heif>=0.15.0

# Audio transcription
# openai-whisper>=20231117   # Local Whisper model; heavy dependency
# SpeechRecognition>=3.10.0  # Google/Sphinx recognizer, WAV only without pydub
# mutagen>=1.47.0            # Audio metadata / duration
# pydub>=0.25.1              # Convert audio formats to WAV for speech_recognition

# Language detection and rich document formats
# langdetect>=1.0.9
# beautifulsoup4>=4.12.0     # HTML parsing
# python-pptx>=0.6.23        # PowerPoint parsing
