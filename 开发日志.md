# 开发日志

## 2026-07-01 初次部署问题修复

### 环境
- 轻量模式：`docker compose -f docker-compose.lightweight.yml up -d`
- Embedding/LLM：智谱 GLM（`open.bigmodel.cn`）

---

### 问题 1：Alembic 迁移 ID 超长导致数据库建表失败

**现象**：数据库为空，登录/注册返回 500。

**原因**：3 个迁移文件的 `revision` ID 超过 Alembic 默认 `VARCHAR(32)` 限制。

**修复**：
- 将以下 3 个迁移文件重命名并缩短 `revision` ID（原文件备份为 `.bak`）：

| 原 revision | 新 revision |
|-------------|-------------|
| `20260615_add_conversations_search_history_evaluation` (48字符) | `20260615_conv_eval` (18字符) |
| `20260615_add_user_display_name_is_active` (40字符) | `20260615_user_active` (20字符) |
| `20260615_add_field_permission_config` (36字符) | `20260615_field_perm` (19字符) |

- 同时更新 `60cfcaa07bfb` 迁移文件的 `down_revision` 指向新的短 ID。
- 涉及文件：
  - `backend/alembic/versions/2026_06_15_1736-20260615_conv_eval.py`（新建）
  - `backend/alembic/versions/2026_06_15_1753-20260615_user_active.py`（新建）
  - `backend/alembic/versions/2026_06_15_2315-20260615_field_perm.py`（新建）
  - `backend/alembic/versions/2026_06_16_1607-60cfcaa07bfb_add_collaboration_and_groups_tables.py`（修改 down_revision）

---

### 问题 2：Nginx 文件上传大小限制

**现象**：上传 PDF/XLSX 大文件报 `413 Request Entity Too Large`。

**原因**：Nginx 默认 `client_max_body_size = 1MB`。

**修复**：`frontend/nginx.conf` 增加 `client_max_body_size 200M;`。

---

### 问题 3：`crypto.randomUUID()` 在非 localhost/HTTPS 环境不可用

**现象**：通过 IP 地址访问时，搜索页面报 `crypto.randomUUID is not a function`。

**原因**：浏览器安全策略，`crypto.randomUUID()` 仅在 `localhost` 或 HTTPS 下可用。

**修复**：`frontend/src/pages/SearchConsole.tsx` 第 243 行，将：
```ts
id: crypto.randomUUID(),
```
替换为手写 UUID 生成器（不依赖浏览器安全上下文）。

---

### 问题 4：Embedding 维度不匹配

**现象**：检索返回完全不相关的内容，`text_chunk_vectors` 表为空。

**原因**：
1. 数据库表 `text_chunk_vectors.embedding` 列定义为 `vector(768)`。
2. 智谱 `embedding-3` 模型不支持 768 维（仅支持 256/512/1024/2048）。
3. 向量写入失败，表始终为空。

**修复**（4 处代码变更）：

| 文件 | 改动 |
|------|------|
| `backend/app/config.py` | 默认 `EMBEDDING_DIMENSION` 768 → 1024 |
| `backend/app/models/vector.py` | `Vector(768)` → `Vector(1024)`，`Vector(512)` → `Vector(1024)` |
| `backend/alembic/versions/20260624_add_pgvector_vectors.py` | 同上 |
| 运行时数据库 | `ALTER TABLE text_chunk_vectors ALTER COLUMN embedding TYPE vector(1024)` |

**注意**：变更后需要删除旧文档并重新上传，向量才能正确写入。

---

---

## 2026-07-02

### 问题 5：代码改了维度但运行中的数据库未同步

**现象**：7-01 修改了 `Vector(768)` → `Vector(1024)` 的代码，Worker 重建后仍报 `expected 768 dimensions, not 1024`，向量始终写不进去。

**原因**：代码层面的 `Vector(1024)` 只在 `CREATE TABLE` 时生效。**已存在的表不会自动变更**。`text_chunk_vectors` 表建表时已是 `vector(768)`，之后即使代码改了，数据库列定义不变。

**技术原理**：pgvector 的 `vector(N)` 在 PostgreSQL 存储引擎层面校验数据长度。INSERT 时逐个 float 对比：
- 列要求 768 个 float → 给了 1024 个 → 直接拒绝
- `ALTER COLUMN ... TYPE vector(1024)` 会触发全表重写（每行从 3076 字节扩到 4100 字节）

**修复**：
```sql
ALTER TABLE text_chunk_vectors ALTER COLUMN embedding TYPE vector(1024);
ALTER TABLE image_frame_vectors ALTER COLUMN embedding TYPE vector(1024);
```

| 文件 | 改动 |
|------|------|
| 数据库运行时 | `ALTER TABLE` 将两张向量表的 embedding 列从 `vector(768)`/`vector(512)` 扩为 `vector(1024)` |

**教训**：代码改 Schema 后，必须同步对运行中的数据库执行 DDL 变更，否则容器重建多少次都没用。


### 问题 6：索引和查询使用不同的分词配置

**现象**：将 BM25 查询从 `simple` 改为 `chinese` 后，检索指标大幅下降。

**原因**：
- 索引时（`ingest_tasks.py`）使用 `to_tsvector("simple", ...)`
- 查询时（`bm25_client.py`）使用 `plainto_tsquery("chinese", ...)`
- 两边分词结果不一致，导致匹配失败

**修复**：
1. 修改 `backend/app/workers/ingest_tasks.py` 第 135 行，将索引分词从 `simple` 改为 `chinese`
2. 重建所有知识库的 BM25 索引（869 个 chunk）

---

### 测试指标对比

#### 测试 1：原始配置（simple 分词，无中文分词）

| 模式 | MRR | Recall@1 | Recall@3 | Recall@5 | Recall@10 | Precision@1 | Precision@3 | Precision@5 | Precision@10 | NDCG@1 | NDCG@3 | NDCG@5 | NDCG@10 |
|------|-----|----------|----------|----------|-----------|-------------|-------------|-------------|--------------|--------|--------|--------|---------|
| hybrid | 1.000 | 0.720 | 0.960 | 0.980 | 1.000 | 1.000 | 0.600 | 0.380 | 0.200 | 1.000 | 1.000 | 0.957 | 0.991 |
| semantic | 1.000 | 0.720 | 0.893 | 1.000 | 1.000 | 1.000 | 0.533 | 0.400 | 0.200 | 1.000 | 0.932 | 0.923 | 0.969 |
| keyword | 1.000 | 0.720 | 0.927 | 0.947 | 0.947 | 1.000 | 0.567 | 0.360 | 0.180 | 1.000 | 1.000 | 1.000 | 1.000 |

#### 测试 2：chinese 分词配置（索引重建前，索引与查询不一致）


| 模式 | MRR | Recall@1 | Recall@3 | Recall@5 | Recall@10 | Precision@1 | Precision@3 | Precision@5 | Precision@10 | NDCG@1 | NDCG@3 | NDCG@5 | NDCG@10 |
|------|-----|----------|----------|----------|-----------|-------------|-------------|-------------|--------------|--------|--------|--------|---------|
| hybrid | 0.767 | 0.467 | 0.753 | 0.840 | 0.927 | 0.600 | 0.367 | 0.280 | 0.170 | 0.600 | 0.601 | 0.673 | 0.786 |
| semantic | 0.583 | 0.167 | 0.740 | 0.773 | 0.980 | 0.300 | 0.367 | 0.240 | 0.190 | 0.300 | 0.422 | 0.446 | 0.660 |
| keyword | 0.720 | 0.500 | 0.620 | 0.753 | 0.827 | 0.500 | 0.300 | 0.220 | 0.140 | 0.500 | 0.685 | 0.727 | 0.796 |

#### 结论

- **测试 2 指标下降原因**：索引和查询使用了不同的分词配置（simple vs chinese），导致匹配失败
- **解决方案**：统一使用 `chinese` 分词，并重建索引。装了zhparser
- **待验证**：测试 3（索引重建后）的指标应该会回升

---

## 2026-07-03

### 问题 7：测试数据重复导致指标下降

**现象**：chinese 分词配置后，指标大幅下降（recall@1: 0.720 → 0.000）。

**原因**：测试脚本每次运行都会上传文件，导致同一个文件被上传 6 次，产生 6 个 doc_id。测试脚本只期望最后一个 doc_id，但检索可能返回前面的 doc_id，导致匹配失败。

**修复**：清理重复文档，只保留每个文件最新的一个。
```sql
DELETE FROM chunks WHERE doc_id IN (
  SELECT d.id FROM documents d
  WHERE d.kb_id = 'a7103b51-a5c3-42a1-9af4-ebabdc3de10f'
  AND d.id NOT IN (
    SELECT DISTINCT ON (filename) id FROM documents
    WHERE kb_id = 'a7103b51-a5c3-42a1-9af4-ebabdc3de10f'
    ORDER BY filename, created_at DESC
  )
);

DELETE FROM documents
WHERE kb_id = 'a7103b51-a5c3-42a1-9af4-ebabdc3de10f'
AND id NOT IN (
  SELECT DISTINCT ON (filename) id FROM documents
  WHERE kb_id = 'a7103b51-a5c3-42a1-9af4-ebabdc3de10f'
  ORDER BY filename, created_at DESC
);
```

**结论**：chinese 分词配置是正确的，之前的指标下降完全是测试数据重复导致的。清理后指标完全恢复。

---

### 测试 3：chinese 分词配置（索引重建后，容器代码未同步）

**问题**：只更新了数据库索引，但容器里的代码还是旧的（`simple`），API 查询仍用 `simple` 分词。

| 模式 | MRR | Recall@1 | Recall@3 | Recall@5 | Recall@10 | Precision@1 | Precision@3 | Precision@5 | Precision@10 | NDCG@1 | NDCG@3 | NDCG@5 | NDCG@10 |
|------|-----|----------|----------|----------|-----------|-------------|-------------|-------------|--------------|--------|--------|--------|---------|
| hybrid | 0.354 | 0.200 | 0.300 | 0.567 | 0.767 | 0.200 | 0.100 | 0.140 | 0.110 | 0.200 | 0.250 | 0.337 | 0.494 |
| semantic | 0.590 | 0.267 | 0.320 | 0.740 | 0.773 | 0.400 | 0.200 | 0.220 | 0.120 | 0.400 | 0.400 | 0.543 | 0.661 |
| keyword | 0.245 | 0.200 | 0.200 | 0.300 | 0.500 | 0.200 | 0.067 | 0.060 | 0.050 | 0.200 | 0.200 | 0.239 | 0.314 |

---

### 测试 4：chinese 分词配置（代码同步到容器后）

**操作**：
1. 将修改后的 `bm25_client.py` 和 `ingest_tasks.py` 复制到容器
2. 重启 app-backend 和 worker
3. 重建所有 chunks 索引（959 个）

| 模式 | MRR | Recall@1 | Recall@3 | Recall@5 | Recall@10 | Precision@1 | Precision@3 | Precision@5 | Precision@10 | NDCG@1 | NDCG@3 | NDCG@5 | NDCG@10 |
|------|-----|----------|----------|----------|-----------|-------------|-------------|-------------|--------------|--------|--------|--------|---------|
| hybrid | 0.328 | 0.000 | 0.253 | 0.687 | 0.893 | 0.000 | 0.200 | 0.220 | 0.160 | 0.000 | 0.177 | 0.337 | 0.527 |
| semantic | 0.473 | 0.067 | 0.320 | 0.740 | 0.773 | 0.200 | 0.200 | 0.220 | 0.120 | 0.200 | 0.308 | 0.464 | 0.575 |
| keyword | 0.312 | 0.000 | 0.353 | 0.587 | 0.853 | 0.000 | 0.233 | 0.200 | 0.140 | 0.000 | 0.269 | 0.353 | 0.512 |

**分析**：
- keyword 模式有明显提升（recall@10: 0.100 → 0.853）
- 但与最初的 simple 配置相比仍有差距（recall@1: 0.720 → 0.000）
- **原因**：zhparser 对英文短词（RAG、BM25）的处理不如 simple 配置，会把 "BM25" 拆成 "bm" + "25"，降低了精确匹配的权重

---

### 测试 5：chinese 分词 + Rerank（硅基流动 bge-reranker-v2-m3）

**操作**：
1. 配置 Rerank API（硅基流动，模型：BAAI/bge-reranker-v2-m3）
2. 清理重复文档（每个文件只保留一个 doc_id）
3. 重建所有 chunks 索引（809 个）
4. 重建 app-backend 和 worker 容器

| 模式 | MRR | Recall@1 | Recall@3 | Recall@5 | Recall@10 | Precision@1 | Precision@3 | Precision@5 | Precision@10 | NDCG@1 | NDCG@3 | NDCG@5 | NDCG@10 |
|------|-----|----------|----------|----------|-----------|-------------|-------------|-------------|--------------|--------|--------|--------|---------|
| hybrid | 1.000 | 0.720 | 0.927 | 0.947 | 0.967 | 1.000 | 0.567 | 0.360 | 0.190 | 1.000 | 0.992 | 0.992 | 0.992 |
| semantic | 1.000 | 0.720 | 0.927 | 0.927 | 0.927 | 1.000 | 0.567 | 0.340 | 0.170 | 1.000 | 0.992 | 0.992 | 0.992 |
| keyword | 0.500 | 0.353 | 0.460 | 0.480 | 0.500 | 0.500 | 0.300 | 0.200 | 0.110 | 0.500 | 0.500 | 0.500 | 0.500 |

**结论**：
- hybrid 和 semantic 模式效果非常好（recall@1=0.720, mrr=1.000）
- keyword 模式效果下降（recall@1=0.353），因为 chinese 分词对英文短词处理不如 simple
- Rerank 有效提升了 hybrid 模式的排序质量

---

### 问题 8：优化文档切分逻辑

**现象**：原切分逻辑按固定 500 字符切分，可能破坏段落完整性，chunk 大小差异大。

**改进**：
1. 按段落切分（`\n\n` 或 `\n`），保持语义完整
2. 合并小段落（min_size=500），避免 chunk 太小
3. 拆分大段落（max_size=800），避免 chunk 太大
4. 表格、目录特殊处理，不拆分
5. 保留 overlap=100，保持上下文关联

**参数**：`target_size=600, min_size=500, max_size=800, overlap=100`

**效果**：chunk 大小差异从 6 倍降到 1.8 倍，无小于 500 字符的 chunks。

**代码位置**：`backend/app/pipelines/document_pipeline.py` 的 `_chunk_text` 方法

---

### 测试 6：UPS 文档检索测试

**测试目的**：测试 samples_2 目录下的 UPS 相关文档（20 个文件）的检索效果。

**测试脚本**：`scripts/eval_retrieval_ups.py`

**测试配置**：
- 知识库：ups-eval-kb
- 文档数量：20 个（PDF/PPT/XLSX）
- 查询数量：9 个语义查询（模拟真实用户问题）
- 检索模式：hybrid、semantic、keyword

**测试结果**：
```bash
cat /tmp/eval_results_final.txt
```

**关键发现**：
1. hybrid 和 semantic 模式效果一致，说明 hybrid 主要依赖语义检索
2. keyword 模式返回 0 结果，因为查询是语义查询，BM25 无法理解
3. 部分查询失败，可能是期望文件需要调整

**相关文件**：
- 测试脚本：`scripts/eval_retrieval_ups.py`
- 测试数据：`samples_2/` 目录
- 切分逻辑：`backend/app/pipelines/document_pipeline.py`

---

## 2026-07-06

### 问题 10：PPT 文件解析失败

**现象**：PPT 文件（.ppt 格式）上传后解析失败，错误信息：`[解析失败: No module named 'pptx']`

**原因**：
1. `python-pptx` 库在 `requirements.txt` 里被注释掉了
2. Worker 容器没有安装 `python-pptx`

**修复**：
1. 取消注释 `backend/requirements.txt` 里的 `python-pptx>=0.6.23`
2. 重新构建 worker 容器：`docker compose -f docker-compose.lightweight.yml build worker`

---

### 问题 11：.ppt 旧格式文件无法用 python-pptx 解析

**现象**：安装 `python-pptx` 后，`.ppt` 文件仍然解析失败，错误：`[解析失败: Package not found at '/tmp/tmp*.ppt']`

**原因**：`python-pptx` 只支持 `.pptx` 格式（Office Open XML），不支持旧的 `.ppt` 格式（二进制格式）。

**修复**：修改 `backend/app/pipelines/document_pipeline.py` 的 `_extract_pptx` 方法，对 `.ppt` 文件先用 `libreoffice` 转换成 `.pptx`，再用 `python-pptx` 解析。

```python
# 2026-07-06: 支持 .ppt 旧格式，先用 libreoffice 转换成 .pptx
if file_path.lower().endswith('.ppt') and not file_path.lower().endswith('.pptx'):
    converted_path = file_path.rsplit('.', 1)[0] + '.pptx'
    subprocess.run(
        ['libreoffice', '--headless', '--convert-to', 'pptx', file_path, '--outdir', '/tmp/'],
        capture_output=True, timeout=60
    )
```

**效果**：
- 09_UPS5000-E 培训资料.ppt：14 个 chunks，11359 字符
- 16_UPS5000 产品介绍.ppt：3 个 chunks，1985 字符

---

### 问题 12：Excel 文件解析内容错位

**现象**：Excel 文件解析后只有页面标题（"工作表'P6'包含列: ..."），没有实际数据内容。

**原因**：`pd.read_excel()` 默认把第一行作为列名（`header=0`），但某些 Excel 文件第一行是空的，导致数据错位。

**修复**：修改 `backend/app/pipelines/excel_pipeline.py` 的 `process` 方法，使用 `header=None` 避免数据错位。

```python
# 2026-07-06: 使用 header=None 避免第一行被当作列名，防止数据错位
sheets = pd.read_excel(file_path, sheet_name=None, header=None)
```

**效果**：Excel 文件的文本内容（如技术要求）现在能正确提取。

---

## 2026-07-08

### 问题 13：LibreOffice 无法处理中文路径的 PPT 文件

**现象**：PPT 文件（.ppt 格式）上传后解析失败，错误信息：`[解析失败: Package not found at '/tmp/tmputsai4f4.ppt']`

**原因**：LibreOffice 在转换 `.ppt` 文件时，如果文件路径包含中文字符或特殊字符（括号、空格等），会报错 "Error: source file could not be loaded"。

**修复**：修改 `backend/app/pipelines/document_pipeline.py` 的 `_extract_pptx` 方法，先将文件复制到简单路径 `/tmp/convert_input.ppt`，再进行转换。

```python
# 修复：LibreOffice 无法处理中文路径，先复制到简单路径
import shutil
tmp_input = '/tmp/convert_input.ppt'
tmp_output = '/tmp/convert_input.pptx'
shutil.copy2(file_path, tmp_input)
subprocess.run(
    ['libreoffice', '--headless', '--convert-to', 'pptx', tmp_input, '--outdir', '/tmp/'],
    capture_output=True, text=True, timeout=60
)
```

**效果**：
- 09_UPS5000-E 培训资料.ppt：14 个 chunks（修复前只有 1 个错误信息）

### 优化 1：Excel Pipeline 重构

**问题**：Excel 文件解析后噪声严重，842 chunks 中 698 个是元数据描述。

**修复**：重构 `excel_pipeline.py`，智能检测表头、过滤空行空列、分离文本提取和切分。

**效果**：

| 文件 | 旧 | 新 | 减少 |
|------|----|----|------|
| 14_机柜底座接口图.xlsx | 842 | 28 | -97% |
| UPS5000-E-200kVA | 325 | 76 | -77% |

### 功能 2：新增文本清洗步骤

**实现**：`base.py` 新增 `_clean_text()`，流程为 保护表格→清洗→还原表格，所有 Pipeline 子类自动继承。

| 清洗步骤 | 示例 |
|----------|------|
| 中文去重 `([一-鿿])\1+` | `为为保保证证` → `为保证` |
| 频次检测删页脚 | 同行出现 ≥3 次自动删除 |
| Page 占位符 | `Page<number>`、`--- 第X页 ---` |
| 孤连字符换行 | `(\w)-\n(\w)` → 合并 |
| `\x0B` 控制字符 | PPT 列表分隔 → `\n` |
| 空表格过滤 | `[TABLE]\n\n[/TABLE]` → 丢弃 |

**调用位置**：`document_pipeline.py`、`excel_pipeline.py` 的 `process()` 中，提取与切分之间。

---

## 2026-07-09

### 问题 16：Excel 解析后内容被清洗逻辑误删

**现象**：Excel 文件中的 "4.相连底座需用螺栓并联，底座需保证至少四角与地面固定" 在提取阶段正常存在，但经过清洗后消失。

**根因**：`base.py` 中的 `_remove_repeating_footers()` 方法检测到该行在 Excel 解析后出现 15 次（每个 P 工作表各一次）、长度在 10~200 字符范围内，判定为「页脚」并全部删除。

**修复**：删除 `_remove_repeating_footers()` 方法。原因：
1. 该逻辑过于激进，将重复出现的有效内容误判为噪声
2. Excel 多行单元格解析后天然会产生重复行（每个工作表一行），不是页脚
3. 违背「宁可放过不能错杀」的清洗原则

**涉及文件**：
- `backend/app/pipelines/base.py`：删除 `_remove_repeating_footers()` 及 `Counter` 导入

---

### 改动 3：评价测试从文件级改为 chunk 级

**背景**：原测试只判断文件是否命中（文件名匹配），无法定位到具体 chunk，导致假阳性（文件名对了但内容不对）和假阴性（相关 chunk 在其他文件）。

**改动**：
1. `eval_retrieval_ups.py` 的 `QUERY_ANNOTATIONS`：支持 `filename_chunkN` 格式标注期望 chunk
2. `evaluate_query()`：从搜索结果提取 `chunk_index`，拼接为 `filename_chunkN` 后匹配
3. `print_results()`：新增 `top chunks` 行显示实际命中的 chunks
4. 逐一修正 10 个 query 的期望标注（从文件名改为 chunk_id）

**当前测试集标注**：

| # | Query | 期望 Chunk | 状态 |
|---|-------|------------|------|
| 1 | SOH显示电池异常 | 02_chunk61 | ✅ |
| 2 | iBOX的RF_Z指示灯不亮了 | 01_chunk12, 02_chunk53, 02_chunk71 | ✅ |
| 3 | 下发电池电压低关机的命令是什么 | 03_chunk170, 05_chunk184 | ✅ |
| 4 | 开关状态线不正常 | 18_chunk638 | ✅ |
| 5 | UPS5000E工作模式_维修旁路是什么 | 09_chunk1 | ✅ |
| 6 | 怎么连接出风边柜电源线 | 15_chunk13, 15_chunk14 | ✅ |
| 7 | MDU显示屏接口有哪些 | 07_chunk83 | ✅（新增） |
| 8 | 母线电压未升起，UPS整流器无法工作 | 07_chunk172, 11_chunk65, 18_chunk307 | ⚠️ 宽松版 |
| 9 | 相连底座用什么并联 | 14_chunk9 | ⚠️ 文件级 |
| 10 | 逆变器异常 | 18_chunk51, 07_chunk201, 11_chunk65 | ⚠️ 文件级 |

---

**现象**：18_告警参考.pdf 出现 154,779 字符的超大 chunk。

**根因**：两个误判 + 一个缺失：

| 问题 | 旧代码 | 新代码 |
|------|--------|--------|
| `is_table` 过宽 | `'[TABLE]' in chunk` | `startswith` + `endswith` 精确匹配 |
| `is_directory` 过宽 | `'.....' in chunk and count > 3` | ≥5行含且 >50% |
| 目录块不拆分 | 直接追加 | 超大目录也走句子切分 |
| 缺兜底逻辑 | 无 | 句切后 >2000字符非表格块按 `\n` 硬切 |

**效果**：18_告警 chunks: 516→773, max: 154,779→1,465


---

## 2026-07-15 Excel Pipeline 全面重构

### 环境
- 轻量模式：`docker compose -f docker-compose.lightweight.yml up -d`
- backend 镜像：全量 `Dockerfile`（含 LibreOffice/tesseract/poppler）
- PostgreSQL：`Dockerfile.pgvector-zh`（含 pgvector + zhparser）

---

### 改动概述

对 `backend/app/pipelines/excel_pipeline.py` 做了全面重写，修复 7 个问题，涉及性能优化、去重、合并单元格处理、chunk 编号等多个方面。

---

### 问题 1：Excel 解析性能差（重复读取文件）

**现象**：解析一个 18 sheet 的 Excel 文件耗时 50 秒。

**原因**：每个 sheet 都独立调用一次 `openpyxl.load_workbook()`（~2.5s/次），18 个 sheet 重复加载 18 次。

**修复**：在 Pipeline 类中增加 `_wb_cache` 字典缓存 Workbook 对象，同一文件的 `load_workbook` 从 18 次降为 1 次。

```python
def __init__(self):
    self._wb_cache: Dict[str, Tuple[Workbook, float]] = {}
```

解析耗时：**50s → 6s**。

---

### 问题 2：chunk_index 硬编码为 0

**现象**：评测脚本按 `filename_chunk{N}` 匹配，所有 chunk 都叫 `_chunk0`，评测全挂。

**原因**：`_generate_tabular()` 和 `_generate_sparse()` 都写死了 `"chunk_index": 0`。

**修复**：在 `process()` 末尾做全局重编号。

---

### 问题 3：forward-fill 与 dropna 顺序错误

**现象**：图纸页 P1~P15 每个产出 238KB 的 chunk，内容全是重复的。

**原因**：
```python
# ❌ 旧代码：forward-fill 先行，把合并单元格填满 1936 格
forward_fill() → dropna() → 删不掉 → 238KB

# ✅ 新代码：dropna 先行，只保留有效 6×12 区域
dropna() → forward_fill() → ~840 chars
```

**修复**：交换 dropna 和 forward-fill 的执行顺序。

---

### 问题 4：列名用 label 访问导致 dropna 后崩溃

**现象**：P 图纸页处理时报 `KeyError: 1`。

**原因**：`_detect_header_row()` 用 `row[c]`（按列名访问）。dropna 后列号不连续（`[0, 2, 7, 17...]`），`row[1]` 找不到列名。

**修复**：统一改为 `df.iloc[i, c]`（position 访问）。

---

### 问题 5：列内类型冲突

**现象**：P 图纸页处理时报 `TypeError: Invalid value 'All rights reserved' for dtype 'float64'`。

**原因**：`_forward_fill_merges()` 把字符串写入 float64 类型的列。

**修复**：在 `_normalize_sheet` 入口加 `df = df.astype(object)`。

---

### 问题 6：forward-fill + sparse 策略冲突导致内容重复

**现象**：图纸页每个 chunk 里同一段"技术要求"重复 10+ 次。

**原因**：sparse 类型不需要 forward-fill，但之前对所有 sheet 都执行了。

**修复**：把 forward-fill 移出 `_normalize_sheet`，只在 **tabular 策略** 下执行。

```python
if strategy == "tabular":
    df = self._forward_fill_merges(df, merge_map)
```

---

### 问题 7：重复读取合并单元格信息

**修复**：新增 `_get_merge_map_in_clean_coords()`，在 dropna 后的坐标系上重建合并映射，只保留有效区域内的合并信息。

---

### 新增功能

#### 字符窗口合并（tabular 策略）

不再按"一行一 chunk"硬切，设置 500~1500 字符的目标窗口：

```python
TARGET_MIN = 500
TARGET_MAX = 1500
```

效果：宽表每行内容多 → 少合并几行；长表每行内容少 → 多合并几行。chunk 大小均匀（300~600 字符）。

#### 全局去重

`_generate_sparse()` 增加行内去重（同一行的重复列值只保留一次）和行间去重（完全相同的内容行只保留第一次）。

#### TOC 点号清洗

在 `base.py` 的 `_clean_text()` 中新增 `_remove_toc_dots()`。清除目录中的点状前导符 `......1`。

---

### 最终产出质量

以 `14_UPS5000&SmartLi 机柜底座接口图.xlsx`（18 个 sheet）为例：

| Sheet | 策略 | chunk 数 | 大小 |
|-------|------|---------|------|
| 封面 | sparse | 1 | ~189 chars |
| 修订记录 | sparse | 1 | ~444 chars |
| 选型目录 | tabular | 8 | 300~600 chars |
| P1~P15 图纸页 | sparse | 15 | ~840 chars |
| **总计** | | **25** | **~15KB** |

无重复内容，无超大 chunk。

---

### 涉及修改的文件

| 文件 | 改动 |
|------|------|
| `backend/app/pipelines/excel_pipeline.py` | 全面重写 |
| `backend/app/pipelines/base.py` | 新增 `_remove_toc_dots()` |
| `backend/Dockerfile` | 加中科大 apt 镜像、腾讯云 pip 镜像、libreoffice |
| `docker-compose.lightweight.yml` | backend 改用全量 Dockerfile |

---

## 2026-07-21 检索优化与最终评测

### 优化

1. **查询改写**：剥离疑问词/停用词（"是什么问题"、"有哪些"、"怎么"等），keyword MRR +24%
2. **离线向量化**：预生成 10 个 query 向量，避免 embedding API 限流（Free 层 1 RPM）
3. **chunk 标注修正**：删除 Q10 不相关的 chunk73

### 改动 4：查询改写从 eval 脚本挪入 web 主链路

**背景**：此前 `rewrite_query` 只存在于 `scripts/eval_retrieval_ups.py` 与 `scripts/eval_offline_ups.py`，在脚本侧对 keyword 模式打 API 前改写 query。web 主链路 `retrieval_service.search` 没有任何改写，导致 **eval 的 keyword 成绩（带改写）不能代表 web 真实行为（不带改写）**，web keyword 模式 MRR 会比 eval 低约 24%。

**修复**：把查询改写提升为 backend 单一来源，web 与 eval 共用同一份实现，避免多份漂移。

| 文件 | 改动 |
|------|------|
| `backend/app/retrieval/query_rewriter.py` | 新建。权威实现 `rewrite_query()`，仅依赖 `re`，模块级预编译正则 |
| `backend/app/services/retrieval_service.py` | import `rewrite_query`；`search()` 中仅 `mode=="keyword"` 时改写 query（对齐 eval：semantic/hybrid 不改写）；模块级提 `logger`，改写命中走 `logger.debug` |
| `scripts/eval_retrieval_ups.py` | 删除本地 `rewrite_query` 副本，改用 `importlib` 按文件路径加载 backend 模块 |
| `scripts/eval_offline_ups.py` | 同上 |

**设计要点**：
- **只对 keyword 改写**：semantic/hybrid 语义检索能理解疑问词，剥离反而损失语义，故不改写。与 eval 原行为严格一致。
- **eval 用 importlib 按文件加载**而非 `from app...import`：避免拖入 SQLAlchemy/DB 等 backend 依赖栈，保持 eval 脚本独立性。
- 改写后 query 同时用于 BM25 与 Rerank，与 eval（改写后打 API）行为一致。
- 行为校验：`SOH显示电池异常是什么问题` → `SOH显示电池异常`；`逆变器异常` → 不变；纯疑问词（如 `有哪些`）回退原值，不返回空串。

**验证方式**：`python3 -c "import ast; ast.parse(open('backend/app/services/retrieval_service.py').read())"` 通过；eval 脚本加载 backend `query_rewriter` 并调用返回正确。

**影响**：web keyword 模式现在与 eval 对齐，eval 的 keyword 指标可代表 web 真实行为。hybrid/semantic 无变化。

### 最终评测结果（hybrid, top_k=30, 带 reranker）

| 指标 | 值 |
|------|----|
| MRR | 0.883 |
| precision@1 | 0.800 |
| recall@3 | 0.485 |
| recall@5 | 0.533 |
| ndcg@3 | 0.913 |

### 命中情况

| Q | 状态 |
|---|------|
| Q1 SOH | ✅ chunk65#1 |
| Q2 RF_Z | ✅ chunk14/55/77 全命中 |
| Q3 电池电压低关机 | ✅ 5 个期望命中 4 个 |
| Q4 输出开关断开 | ✅ chunk655#1, chunk654#2（改写后从 total=1→16） |
| Q5 维修旁路 | ✅ chunk1/2 全命中 |
| Q6 出风边柜 | ✅ chunk13/14 全命中 |
| Q7 MDU接口 | ✅ chunk84#2, chunk83#5 |
| Q8 母线电压 | ✅ chunk178/67/308 全命中 |
| Q9 支脚机柜 | ✅ chunk2/13 全命中 |
| Q10 电池冷启动 | ✅ chunk164#1 |

### 未处理问题

- Q2 keyword total=0（RF_Z 特殊字符，BM25 无法分词）

## 2026-07-22 测试集扩展与 expected_file 标注验证

### 背景

评测工作台中发现一个5000条数据的"UPS"数据集，指向只有4个文档的`UPS知识库`（非20个文档的`ups-eval-kb`）。该数据集的 expected_file 使用 `文件名#p{页码}` 格式，与系统实际 chunk UUID 无法匹配，导致所有检索指标为0。确认其构建者使用了不同的按页切分逻辑，与本系统 pipeline 无关。

### 测试集扩展：新增10条 query

原 `eval_retrieval_ups.py` 只有10条 query，覆盖10/20个文档。新增10条覆盖6个原先未覆盖的文档（04/06/08/10/17/19/20），同时补全覆盖缺口，使20条覆盖18/20文档。

| 新增 | 类型 | 文档 | expected数 |
|------|------|------|-----------|
| Q11 | safety | 19_安全须知 | 1 |
| Q12 | troubleshooting | 13_FAQ | 2 |
| Q13 | procedural | 11_维护指南 | 2 |
| Q14 | parameter | 06_BF电池柜 | 1 |
| Q15 | factoid | 10_监控模块 | 1 |
| Q16 | multi-doc(密码) | 10+11+20 | 3 |
| Q17 | procedural | 17_反灌保护卡 | 1 |
| Q18 | procedural | 20_干接点扩展卡 | 1 |
| Q19 | procedural | 04_208V快速指南 | 2 |
| Q20 | parameter | 08_SM半柜高 | 2 |

新增 `--run-new-only` 参数支持只跑新增query。

### 新增10条检索结果

跑完10条新增query后发现有标注不完整问题：

**明显漏标的例子：**
- Q11"安装环境要求"：only标了`19_安全须知_chunk0`，但`05_208V手册_chunk46`和`03_武汉工行_chunk43`也有完整的"1.4 安装环境要求"小节，均未被标注
- Q20"螺栓规格"：`08_chunk1`实际不含螺栓表，螺栓表在`08_chunk2`，标错

**发现：** metric偏低的原因不止是检索能力问题，还有 expected_file 标注不全导致 recall 虚低。

### 验证方法

用 `chunks_result/` 目录的实际chunk内容逐条验证——对每条query：
1. 列出检索返回的top-5 chunk的实际内容
2. 人工判断是否语义上回答了问题
3. 把相关但漏标的补进expected_file
4. 标错的修正

（Q11验证中，Q12-Q20待继续）

### 前端样式优化

- 优化了前端页面样式，后端接口未做改动
